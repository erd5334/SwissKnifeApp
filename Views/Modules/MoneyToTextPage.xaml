<Page x:Class="SwissKnifeApp.Views.Modules.MoneyToTextPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SwissKnifeApp.Views.Modules"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MoneyToTextPage">

  <Grid Margin="40">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="💸 Parayı Yazıya Çevir" FontSize="22" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20" Grid.Row="0"/>

    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,12" VerticalAlignment="Top">
      <TextBox x:Name="txtAmount" Width="160" Height="32" FontSize="16" Margin="0,0,12,0" VerticalContentAlignment="Center"/>
      <ComboBox x:Name="cmbCurrency" Width="100" Height="32" FontSize="16" SelectedIndex="0" Margin="0,0,12,0">
        <ComboBoxItem Content="TRY"/>
        <ComboBoxItem Content="USD"/>
      </ComboBox>
      <ComboBox x:Name="cmbLanguage" Width="100" Height="32" FontSize="16" SelectedIndex="0" Margin="0,0,12,0">
        <ComboBoxItem Content="Türkçe"/>
        <ComboBoxItem Content="İngilizce"/>
      </ComboBox>
      <Button x:Name="btnConvert" Content="Yazıya Çevir" Width="120" Height="32" FontSize="16" Click="BtnConvert_Click"/>
    </StackPanel>

    <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,12" VerticalAlignment="Top" >
      <ComboBox x:Name="cmbCasing" Width="120" Height="32" FontSize="16" SelectedIndex="2" Margin="0,0,12,0">
        <ComboBoxItem Content="Büyük Harf"/>
        <ComboBoxItem Content="Küçük Harf"/>
        <ComboBoxItem Content="İlk Harf Büyük"/>
      </ComboBox>
      <CheckBox x:Name="chkNoSpaces" Content="Boşluksuz" FontSize="16" Margin="0,0,12,0"/>
      <TextBox x:Name="txtSeparator" Width="60" Height="32" FontSize="16" Margin="0,0,12,0" VerticalContentAlignment="Center"/>
      <TextBlock Text="Ayraç" FontSize="16" VerticalAlignment="Center"/>
            <Button x:Name="btnCopy" Content="📋 Kopyala" Width="100" Height="32" FontSize="12" Margin="15,0,0,0" Click="btnCopy_Click"/>
        </StackPanel>

    <Border Grid.Row="3" Padding="16" Background="#F9FAFB" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1">
      <TextBlock x:Name="txtResult" FontSize="18" Foreground="#374151" TextWrapping="Wrap"/>
    </Border>
  </Grid>
</Page>
