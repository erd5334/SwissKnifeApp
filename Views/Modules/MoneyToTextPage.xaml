<Page x:Class="SwissKnifeApp.Views.Modules.MoneyToTextPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1100"
      Background="{DynamicResource AppBackground}"
      Title="Parayı Yazıya Çevir">

    <Page.Resources>
        <!-- Money themed accent -->
        <SolidColorBrush x:Key="AccentBrush" Color="#2ECC71"/>
        <SolidColorBrush x:Key="AccentBrush2" Color="#27AE60"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Modern Header -->
        <Border Grid.Row="0" Padding="25,20" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconLucide Kind="Wallet" Width="30" Height="30" VerticalAlignment="Center" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="Parayı Yazıya Çevirici" FontSize="24" FontWeight="Bold" Margin="15,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource HeaderText}"/>
                </StackPanel>
                <TextBlock Text="Sayısal miktar değerlerini otomatik olarak metin formatına dönüştürün" FontSize="14" Foreground="{DynamicResource SecondaryText}" Margin="45,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Input Settings -->
                    <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="Miktar ve Birim Ayarları" Style="{StaticResource SectionHeader}"/>
                            
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="Giriş Miktarı" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,8" FontSize="12"/>
                                    <TextBox x:Name="txtAmount" Height="40" FontSize="16" VerticalContentAlignment="Center" Style="{StaticResource RoundedTextBox}" mah:TextBoxHelper.Watermark="1250.75" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="Para Birimi" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,8" FontSize="12"/>
                                    <ComboBox x:Name="cmbCurrency" Height="40" SelectedIndex="0">
                                        <ComboBoxItem Content="TRY (Türk Lirası)"/>
                                        <ComboBoxItem Content="USD (Dolar)"/>
                                        <ComboBoxItem Content="EUR (Euro)"/>
                                        <ComboBoxItem Content="GBP (Sterlin)"/>
                                        <ComboBoxItem Content="RUB (Ruble)"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,15,0,0">
                                <TextBlock Text="Dönüştürme Dili" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,8" FontSize="12"/>
                                <ComboBox x:Name="cmbLanguage" Height="40" SelectedIndex="0" SelectedValuePath="Tag">
                                    <ComboBoxItem Tag="Türkçe">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Translate" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Türkçe" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </ComboBoxItem>
                                    <ComboBoxItem Tag="İngilizce">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Translate" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="İngilizce" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </ComboBoxItem>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Format Settings -->
                    <Border Grid.Column="1" Style="{StaticResource Card}" Margin="10,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Formatlama" Style="{StaticResource SectionHeader}"/>
                            
                            <TextBlock Text="Metin Stili" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,8" FontSize="12"/>
                            <ComboBox x:Name="cmbCasing" Height="40" Margin="0,0,0,15" SelectedIndex="2">
                                <ComboBoxItem Content="BÜYÜK HARF"/>
                                <ComboBoxItem Content="küçük harf"/>
                                <ComboBoxItem Content="İlk Harfler Büyük"/>
                            </ComboBox>

                            <TextBlock Text="Kuruş Ayıracı" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,8" FontSize="12"/>
                            <TextBox x:Name="txtSeparator" Height="40" Style="{StaticResource RoundedTextBox}" mah:TextBoxHelper.Watermark="Örn: 'virgül'" Margin="0,0,0,15" />

                            <CheckBox x:Name="chkNoSpaces" Content="Boşlukları Kaldır" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Actions -->
                <Grid Margin="0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button x:Name="btnConvert" Click="BtnConvert_Click" Style="{StaticResource AccentButtonStyle}" Height="50" Padding="40,0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconLucide Kind="Zap" Width="20" Height="20" Margin="0,0,12,0"/>
                            <TextBlock Text="Yazıya Çevir" FontSize="16" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="1" x:Name="btnCopy" Click="btnCopy_Click" Style="{StaticResource OutlineButtonStyle}" Height="50" Padding="25,0" Margin="15,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="ContentCopy" Width="18" Height="18" Margin="0,0,10,0"/>
                            <TextBlock Text="Sonucu Kopyala"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Result Area -->
                <Border Style="{StaticResource Card}" Padding="0" ClipToBounds="True">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="{DynamicResource AppBackground}" Padding="15,10" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="Çevrilmiş Metin" FontWeight="Bold" Foreground="{DynamicResource HeaderText}"/>
                        </Border>
                        <Border Grid.Row="1" Background="Transparent" MinHeight="150" Padding="20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="txtResult" FontSize="20" Foreground="{DynamicResource HeaderText}" FontWeight="SemiBold" TextWrapping="Wrap" Text="Dönüştürme sonucu burada görünecektir..."/>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Border>
                
                <!-- Info Box -->
                <Border Margin="0,20,0,0" Padding="20" CornerRadius="12" Background="{DynamicResource AppBackground}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="InformationOutline" Width="20" Height="20" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="Finansal dökümanlar ve makbuzlar için standart yazı formatlarını kullanır." Margin="15,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" FontSize="13"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>