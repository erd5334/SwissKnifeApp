<UserControl x:Class="SwissKnifeApp.Views.Modules.DataAnalysisPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{DynamicResource AppBackground}">

    <!-- Page-level accent override (DataAnalysis = Teal) -->
    <UserControl.Resources>
        <SolidColorBrush x:Key="ModuleAccent" Color="#00897B"/>
        
        <Style x:Key="AnalizTabHeader" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>

        <Style x:Key="ModernDataGrid" TargetType="DataGrid" BasedOn="{StaticResource MahApps.Styles.DataGrid}">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource AppBackground}"/>
            <Setter Property="RowBackground" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AppForeground}"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Modern Header -->
        <Border Grid.Row="0" Padding="25,20" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="ChartBox" Width="30" Height="30" VerticalAlignment="Center" Foreground="{StaticResource ModuleAccent}"/>
                    <TextBlock Text="Veri Analiz Merkezi" FontSize="24" FontWeight="Bold" Margin="15,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource HeaderText}"/>
                </StackPanel>
                <TextBlock Text="CSV/Excel verilerini analiz edin, görselleştirin ve raporlar oluşturun" FontSize="14" Foreground="{DynamicResource SecondaryText}" Margin="45,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <mah:MetroAnimatedSingleRowTabControl Grid.Row="1" Margin="20" mah:HeaderedControlHelper.HeaderFontSize="15">
            
            <!-- Temel Analiz -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="ChartBar" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Temel Analiz" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Top Toolbar Card -->
                        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource AccentButtonStyle}" Click="BtnOpenFile_Click" Padding="15,8">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="FolderOpen" Width="16" Height="16" Margin="0,0,10,0"/>
                                            <TextBlock Text="Dosya Aç" FontWeight="Bold"/>
                                        </StackPanel>
                                    </Button>
                                    <Separator Margin="15,0" Width="1" Background="{DynamicResource CardBorderBrush}"/>
                                    <TextBlock Text="Grafik:" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <ComboBox x:Name="cmbChartType" Width="180" Height="36" SelectionChanged="CmbChartType_SelectionChanged">
                                        <ComboBoxItem Content="Bar Grafiği" IsSelected="True"/>
                                        <ComboBoxItem Content="Pasta Grafiği"/>
                                        <ComboBoxItem Content="Çizgi Grafiği"/>
                                        <ComboBoxItem Content="Dağılım Grafiği"/>
                                    </ComboBox>
                                </StackPanel>
                                <Button Grid.Column="1" Style="{StaticResource OutlineButtonStyle}" Click="BtnExportReport_Click" Padding="15,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="FileDocumentOutline" Width="16" Height="16" Margin="0,0,10,0"/>
                                        <TextBlock Text="Rapor Oluştur"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="400"/>
                            </Grid.ColumnDefinitions>

                            <!-- Data Table Card -->
                            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,20,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Magnify" Width="18" Height="18" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" Margin="0,0,10,0"/>
                                            <TextBox x:Name="txtFilter" Style="{StaticResource RoundedTextBox}" Width="300" Height="36" TextChanged="TxtFilter_TextChanged" mah:TextBoxHelper.Watermark="Tabloda ara..."/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <CheckBox x:Name="chkPagination" Content="Sayfalama" VerticalAlignment="Center" Margin="0,0,15,0" Checked="ChkPagination_Changed" Unchecked="ChkPagination_Changed"/>
                                            <TextBlock Text="Satır Sayısı:" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" Margin="0,0,8,0"/>
                                            <TextBox x:Name="txtPageSize" Width="60" Text="50" Height="32" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>

                                    <DataGrid x:Name="dataGrid" Grid.Row="1" MinHeight="400" Style="{StaticResource ModernDataGrid}"/>

                                    <StackPanel Grid.Row="2" Margin="0,15,0,0">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" x:Name="paginationPanel" Visibility="Collapsed" Margin="0,0,0,15">
                                            <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnFirstPage_Click" Width="36" Height="36" Margin="0,0,5,0">
                                                <iconPacks:PackIconMaterial Kind="ChevronDoubleLeft" Width="14" Height="14"/>
                                            </Button>
                                            <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnPrevPage_Click" Width="36" Height="36" Margin="0,0,10,0">
                                                <iconPacks:PackIconMaterial Kind="ChevronLeft" Width="14" Height="14"/>
                                            </Button>
                                            <TextBlock x:Name="txtPageInfo" Text="Sayfa 1 / 1" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}"/>
                                            <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnNextPage_Click" Width="36" Height="36" Margin="10,0,5,0">
                                                <iconPacks:PackIconMaterial Kind="ChevronRight" Width="14" Height="14"/>
                                            </Button>
                                            <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnLastPage_Click" Width="36" Height="36">
                                                <iconPacks:PackIconMaterial Kind="ChevronDoubleRight" Width="14" Height="14"/>
                                            </Button>
                                        </StackPanel>

                                        <Border Background="{DynamicResource AppBackground}" Padding="10" CornerRadius="8">
                                            <WrapPanel>
                                                <TextBlock Text="Sütun Seç:" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" Margin="0,0,10,0"/>
                                                <ComboBox x:Name="cmbColumns" Width="140" Height="32" Margin="0,0,15,0"/>
                                                
                                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnStats_Click" Margin="0,0,8,0" Height="32" Padding="12,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <iconPacks:PackIconMaterial Kind="Calculator" Width="14" Height="14" Margin="0,0,8,0"/>
                                                        <TextBlock Text="İstatistik"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnBoxPlot_Click" Margin="0,0,8,0" Height="32" Padding="12,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <iconPacks:PackIconMaterial Kind="ChartBoxOutline" Width="14" Height="14" Margin="0,0,8,0"/>
                                                        <TextBlock Text="Box Plot"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnViolinPlot_Click" Margin="0,0,8,0" Height="32" Padding="12,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <iconPacks:PackIconMaterial Kind="ChartBellCurve" Width="14" Height="14" Margin="0,0,8,0"/>
                                                        <TextBlock Text="Violin"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnCorrelation_Click" Margin="0,0,8,0" Height="32" Padding="12,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <iconPacks:PackIconMaterial Kind="ChartScatterPlot" Width="14" Height="14" Margin="0,0,8,0"/>
                                                        <TextBlock Text="Korelasyon"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnExportChart_Click" Height="32" Padding="12,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <iconPacks:PackIconMaterial Kind="ImageOutline" Width="14" Height="14" Margin="0,0,8,0"/>
                                                        <TextBlock Text="PNG"/>
                                                    </StackPanel>
                                                </Button>
                                            </WrapPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Side Panel (Plot & Stats) -->
                            <StackPanel Grid.Column="1">
                                <Border Style="{StaticResource Card}" Height="350" Margin="0,0,0,20">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="Grafik Görünümü" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                                        <oxy:PlotView x:Name="oxyChart" Grid.Row="1" Background="Transparent" Foreground="{DynamicResource AppForeground}"/>
                                    </Grid>
                                </Border>

                                <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                        <TextBlock Text="Analiz Özet" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                                        <Border Background="{DynamicResource InputBackground}" CornerRadius="8" Padding="15">
                                            <TextBlock x:Name="txtStatsResult" FontFamily="Consolas" FontSize="13" Foreground="{DynamicResource AppForeground}" TextWrapping="Wrap" Text="Lütfen bir sütun seçip analiz butonuna basın."/>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Orta Analiz -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="ChartHistogram" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Orta Analiz" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Histogram Sütun:" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" Margin="0,0,10,0"/>
                            <ComboBox x:Name="cmbHistogramColumn" Width="200" Height="36" Margin="0,0,20,0"/>
                            <Button Style="{StaticResource AccentButtonStyle}" Click="BtnHistogram_Click" Padding="20,8">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="ChartBar" Width="16" Height="16" Margin="0,0,10,0"/>
                                    <TextBlock Text="Histogram Oluştur" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <oxy:PlotView x:Name="histogramChart" Background="Transparent"/>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Histogram Bilgisi" FontWeight="Bold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                            <TextBlock x:Name="txtHistogramInfo" FontFamily="Consolas" Foreground="{DynamicResource AppForeground}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- İleri Analiz (Regresyon) -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="ChartLineVariant" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="İleri Analiz" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="X Sütunu:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource SecondaryText}"/>
                            <ComboBox x:Name="cmbXColumn" Width="160" Height="36" Margin="0,0,20,0"/>
                            <TextBlock Text="Y Sütunu:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource SecondaryText}"/>
                            <ComboBox x:Name="cmbYColumn" Width="160" Height="36" Margin="0,0,20,0"/>
                            <Button Style="{StaticResource AccentButtonStyle}" Click="BtnRegression_Click" Padding="20,8">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Calculator" Width="16" Height="16" Margin="0,0,10,0"/>
                                    <TextBlock Text="Regresyon Hesapla" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <oxy:PlotView x:Name="regressionChart" Background="Transparent"/>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Regresyon Sonuçları" FontWeight="Bold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                            <TextBlock x:Name="txtRegressionInfo" FontFamily="Consolas" Foreground="{DynamicResource AppForeground}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Veri Temizleme -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="Broom" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Veri Temizleme" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Veri Kalitesi Analizi" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Sütun Seç:" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" Margin="0,0,10,0"/>
                                <ComboBox x:Name="cmbCleaningColumn" Width="200" Height="36" Margin="0,0,20,0"/>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnAnalyzeQuality_Click" Padding="20,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Magnify" Width="16" Height="16" Margin="0,0,10,0"/>
                                        <TextBlock Text="Kalite Analizi" FontWeight="Bold"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Düzeltme İşlemleri" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,12"/>
                            <WrapPanel>
                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnFillMissing_Click" Margin="0,0,10,10" Padding="15,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="DatabaseEdit" Width="14" Height="14" Margin="0,0,10,0"/>
                                        <TextBlock Text="Eksik Verileri Doldur"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnRemoveOutliers_Click" Margin="0,0,10,10" Padding="15,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="DeleteSweep" Width="14" Height="14" Margin="0,0,10,0"/>
                                        <TextBlock Text="Aykırı Değerleri Kaldır"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnRemoveDuplicates_Click" Margin="0,0,10,10" Padding="15,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="ContentDuplicate" Width="14" Height="14" Margin="0,0,10,0"/>
                                        <TextBlock Text="Tekrarları Sil"/>
                                    </StackPanel>
                                </Button>
                            </WrapPanel>
                            
                            <TextBlock Text="Dışa Aktarma Araçları" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,10,0,12"/>
                            <WrapPanel>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnExportExcel_Click" Margin="0,0,10,0" Height="36" Padding="15,0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="FileExcel" Width="14" Height="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Excel"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnExportJson_Click" Margin="0,0,10,0" Height="36" Padding="15,0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="CodeJson" Width="14" Height="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="JSON"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnExportHtml_Click" Margin="0,0,10,0" Height="36" Padding="15,0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="LanguageHtml5" Width="14" Height="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="İnteraktif HTML"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnCreateDashboard_Click" Height="36" Padding="15,0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="ViewDashboard" Width="14" Height="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Dashboard"/>
                                    </StackPanel>
                                </Button>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}">
                        <ScrollViewer MaxHeight="200">
                            <TextBlock x:Name="txtCleaningResult" FontFamily="Consolas" Foreground="{DynamicResource AppForeground}" TextWrapping="Wrap" Text="Kalite analizi veya temizleme işlemi sonrası sonuçlar burada görünecek."/>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Pivot ve Gruplama -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="TablePivot" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Pivot &amp; Grup" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Pivot Tablo Oluştur" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,12"/>
                            <WrapPanel>
                                <StackPanel Margin="0,0,15,8">
                                    <TextBlock Text="Satır (Row):" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbPivotRow" Width="120" Height="32"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,15,8">
                                    <TextBlock Text="Sütun (Col):" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbPivotColumn" Width="120" Height="32"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,15,8">
                                    <TextBlock Text="Değer (Val):" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbPivotValue" Width="120" Height="32"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,20,8">
                                    <TextBlock Text="İşlem:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbPivotAgg" Width="100" Height="32" SelectedIndex="0">
                                        <ComboBoxItem Content="Sum"/>
                                        <ComboBoxItem Content="Avg"/>
                                        <ComboBoxItem Content="Count"/>
                                        <ComboBoxItem Content="Min"/>
                                        <ComboBoxItem Content="Max"/>
                                    </ComboBox>
                                </StackPanel>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnPivot_Click" Height="32" Padding="20,0" VerticalAlignment="Bottom" Margin="0,0,0,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="PlayCircle" Width="14" Height="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Oluştur"/>
                                    </StackPanel>
                                </Button>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Gruplama ve Agregasyon" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,12"/>
                            <WrapPanel>
                                <StackPanel Margin="0,0,15,8">
                                    <TextBlock Text="Grup Sütunu:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbGroupColumn" Width="150" Height="32"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,15,8">
                                    <TextBlock Text="Değer Sütunu:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbGroupValue" Width="150" Height="32"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,20,8">
                                    <TextBlock Text="İşlem:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbGroupAgg" Width="100" Height="32" SelectedIndex="0">
                                        <ComboBoxItem Content="Sum"/>
                                        <ComboBoxItem Content="Avg"/>
                                        <ComboBoxItem Content="Count"/>
                                        <ComboBoxItem Content="Min"/>
                                        <ComboBoxItem Content="Max"/>
                                    </ComboBox>
                                </StackPanel>
                                <Button Style="{StaticResource AccentButtonStyle}" Click="BtnGroupBy_Click" Height="32" Padding="20,0" VerticalAlignment="Bottom" Margin="0,0,0,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Function" Width="14" Height="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Grupla"/>
                                    </StackPanel>
                                </Button>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}">
                        <DataGrid x:Name="pivotResultGrid" MinHeight="300" Style="{StaticResource ModernDataGrid}"/>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Zaman Serisi -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="ChartTimeline" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Zaman Serisi" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Margin="0,0,20,0">
                                <TextBlock Text="Veri Sütunu:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbTimeSeriesColumn" Width="160" Height="36"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,20,0">
                                <TextBlock Text="Hareketli Ort. (Window):" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtMovingAvgWindow" Width="60" Text="3" Height="36" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,25,0">
                                <TextBlock Text="Tahmin (Steps):" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtForecastSteps" Width="60" Text="5" Height="36" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                            <Button Style="{StaticResource AccentButtonStyle}" Click="BtnTimeSeries_Click" Padding="25,0" Height="36" VerticalAlignment="Bottom">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="PlayCircle" Width="16" Height="16" Margin="0,0,10,0"/>
                                    <TextBlock Text="Analiz Et" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <oxy:PlotView x:Name="timeSeriesChart" Background="Transparent"/>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}">
                        <TextBlock x:Name="txtTimeSeriesInfo" FontFamily="Consolas" Foreground="{DynamicResource AppForeground}" TextWrapping="Wrap" Text="Zaman serisi ve tahmin sonuçları burada görünecek."/>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Isı Haritası -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="GridLarge" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Isı Haritası" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal">
                            <Button Style="{StaticResource AccentButtonStyle}" Click="BtnHeatmap_Click" Padding="25,10">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="GridLarge" Width="18" Height="18" Margin="0,0,10,0"/>
                                    <TextBlock Text="Isı Haritası Oluştur" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                            <TextBlock Text="Tüm sayısal sütunlar arasındaki korelasyon matrisini görselleştirir." VerticalAlignment="Center" Margin="20,0,0,0" Foreground="{DynamicResource SecondaryText}" FontStyle="Italic"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Style="{StaticResource Card}">
                        <oxy:PlotView x:Name="heatmapChart" Background="Transparent"/>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Veri Karşılaştırma -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Style="{StaticResource AnalizTabHeader}">
                        <iconPacks:PackIconMaterial Kind="CompareHorizontal" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Karşılaştır" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Dosya Karşılaştırma Araçları" FontWeight="SemiBold" Foreground="{DynamicResource HeaderText}" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnLoadSecondFile_Click" Padding="20,8">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="FileUpload" Width="16" Height="16" Margin="0,0,10,0"/>
                                        <TextBlock Text="2. Dosyayı Yükle"/>
                                    </StackPanel>
                                </Button>
                                <TextBlock x:Name="txtSecondFileInfo" Text="Henüz dosya yüklenmedi" VerticalAlignment="Center" Margin="20,0,0,0" Foreground="{DynamicResource SecondaryText}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
                        <WrapPanel>
                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Anahtar Sütun:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbKeyColumn" Width="160" Height="36"/>
                            </StackPanel>
                            
                            <Button Style="{StaticResource OutlineButtonStyle}" Click="BtnCompare_Click" Height="36" VerticalAlignment="Bottom" Padding="20,0" Margin="0,0,20,10">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="CompareHorizontal" Width="14" Height="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Karşılaştır"/>
                                </StackPanel>
                            </Button>

                            <StackPanel Margin="0,0,20,10">
                                <TextBlock Text="Birleştirme Tipi:" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbMergeType" Width="120" Height="36" SelectedIndex="0">
                                    <ComboBoxItem Content="Inner Join"/>
                                    <ComboBoxItem Content="Left Join"/>
                                    <ComboBoxItem Content="Outer Join"/>
                                </ComboBox>
                            </StackPanel>

                            <Button Style="{StaticResource AccentButtonStyle}" Click="BtnMerge_Click" Height="36" VerticalAlignment="Bottom" Padding="25,0" Margin="0,0,0,10">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Merge" Width="16" Height="16" Margin="0,0,10,0"/>
                                    <TextBlock Text="Dosyaları Birleştir" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}">
                        <ScrollViewer>
                            <TextBlock x:Name="txtComparisonResult" FontFamily="Consolas" Foreground="{DynamicResource AppForeground}" TextWrapping="Wrap" Text="Karşılaştırma sonuçları burada raporlanacak."/>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
        </mah:MetroAnimatedSingleRowTabControl>
    </Grid>
</UserControl>
